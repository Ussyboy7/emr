# Django
DJANGO_SECRET_KEY=staging-secret-change-me
DJANGO_DEBUG=False
DJANGO_ENV=stag
ALLOWED_HOSTS=172.16.0.46,backend_stag,localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://172.16.0.46,http://172.16.0.46:4647,http://backend_stag:8001,http://localhost,http://127.0.0.1:4647

# Database (matches docker-compose.stag.yml)
# Note: Use service name from docker-compose, not container name
DB_ENGINE=postgres
DB_NAME=emr_db_stag
DB_USER=emradmin
DB_PASSWORD=emradmin
DB_HOST=postgres
DB_PORT=5432

# Redis / Celery
# Note: Use service name from docker-compose, not container name
REDIS_HOST=redis
REDIS_PORT=6379
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# JWT
JWT_ACCESS_MINUTES=30
JWT_REFRESH_DAYS=7

# Frontend integration
CORS_ALLOWED_ORIGINS=http://172.16.0.46:4647,http://localhost:4647,http://127.0.0.1:4647
MAX_UPLOAD_SIZE_MB=10

# Timezone
TIME_ZONE=Africa/Lagos

# Pagination
PAGINATION_PAGE_SIZE=20

